import { dateFormat } from './date';
import { parseString, parseObject } from './parse';
import { formatNumber } from './number';
import { getType, checkType } from './type';
import { regEx, regMobile, regEmail, regCard } from './regexp';

export default {
    dateFormat,
    parseString,
    parseObject,
    formatNumber,
    dateFormat,
    getType,
    checkType,
    regEx,
    regMobile,
    regEmail,
    regCard,
};
